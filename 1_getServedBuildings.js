function getServedBuildings(buildingCount, routerLocation, routerRange) {

    let serverdBuildings = 0 
    let provides = []
    const position = routerLocation.map(p => p)
    const range = routerRange.map(r => r)

    for (let = i; i <= routerLocation.length;i++) {
        
    }
    
}


getServedBuildings([1,2,1,2,2,], [3,1],[1,2])